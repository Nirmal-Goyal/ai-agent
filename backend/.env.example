# No API key required. Rule-based fixes only (no LLM at runtime).
# OPENAI_API_KEY=sk-...  # Unused - kept for future optional LLM integration

# Copy this file to .env and fill values as needed.

# For push (fallback when user has not signed in via OAuth):
# GITHUB_TOKEN=ghp_...

# OAuth (Sign in with GitHub) - create app at https://github.com/settings/developers
# GITHUB_CLIENT_ID=...
# GITHUB_CLIENT_SECRET=...
# BACKEND_URL=https://your-backend.onrender.com
# FRONTEND_URL=https://aiagent-blush.vercel.app

# Max retry iterations (default 5)
# RETRY_LIMIT=5
