# No API key required. Rule-based fixes only (no LLM at runtime).
# OPENAI_API_KEY=sk-...  # Unused - kept for future optional LLM integration

# For pushing fixes to GitHub (required for private repos, recommended for owned repos)
# GITHUB_TOKEN=ghp_...

# Max retry iterations (default 5)
# RETRY_LIMIT=5
